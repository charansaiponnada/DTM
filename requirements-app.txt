# DTM Drainage AI — Streamlit dashboard dependencies
# Pin to ranges that are tested together; update when deploying to new Python

streamlit>=1.35,<2.0
streamlit-folium>=0.20,<1.0
folium>=0.17,<1.0
plotly>=5.0,<7.0

# Geospatial
geopandas>=0.14,<2.0
rasterio>=1.3,<2.0
pyproj>=3.6,<4.0
shapely>=2.0,<3.0

# Numerics / plotting (usually already installed)
numpy>=1.24
matplotlib>=3.7
pandas>=2.0
