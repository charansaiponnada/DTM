# ── Core Geospatial ──────────────────────────────────────────────────
laspy[laszip]>=2.5.0         # Read/write LAS/LAZ point clouds
pdal>=3.0.0                  # Point cloud processing pipelines
pyproj>=3.6.0                # CRS transformations
shapely>=2.0.0               # Geometry operations
fiona>=1.9.0                 # Vector I/O
geopandas>=0.14.0            # Geospatial DataFrames
rasterio>=1.3.0              # Raster I/O + COG writing
rio-cogeo>=5.0.0             # Cloud-Optimized GeoTIFF utilities
richdem>=0.3.4               # Hydrological terrain analysis
whitebox>=2.3.0              # WhiteboxTools Python wrapper (hydrology)

# ── Scientific / ML ──────────────────────────────────────────────────
numpy>=1.26.0
scipy>=1.12.0
scikit-learn>=1.4.0          # Random Forest, preprocessing
xgboost>=2.0.0               # Gradient boosting for waterlogging prediction
torch>=2.2.0                 # PointNet deep learning classifier
torch-geometric>=2.5.0       # Graph Neural Networks (flow prediction)
open3d>=0.18.0               # 3D point cloud visualization & processing

# ── DTM / Interpolation ──────────────────────────────────────────────
scipy>=1.12.0                # IDW / interpolation
pykrige>=1.7.0               # Kriging interpolation
matplotlib>=3.8.0
seaborn>=0.13.0

# ── Hydraulic / Flow ─────────────────────────────────────────────────
pysheds>=0.3.5               # Watershed & flow analysis (pure-Python)
networkx>=3.2.0              # Drainage network graph modeling

# ── Utilities ────────────────────────────────────────────────────────
pyyaml>=6.0.1
tqdm>=4.66.0
loguru>=0.7.2
click>=8.1.0                 # CLI
rich>=13.7.0                 # Pretty terminal output
joblib>=1.3.0                # Parallel processing
pandas>=2.2.0
